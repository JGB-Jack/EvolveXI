import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function SessionsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Sessions</CardTitle>
        <CardDescription>
          Create and run assessment sessions here. Coming in Phase 4–5.
        </CardDescription>
      </CardHeader>
    </Card>
  );
}
